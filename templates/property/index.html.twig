{% extends 'base.html.twig' %}

{% block title 'Acheter' %}

{% block body %}

<h2>Acheter un bien </h2>

<div class="container">
     <div class="row">
     
         {% for property in properties %} 

               <div class="card col-4">
                    <img src="..." class="card-img-top" alt="...">
                    <div class="card-body">
                         <h5 class="card-title">  <a href="{{path('property_show', { id: property.id, slug: property.slug }) }}"> {{ property.name }}</a> </h5>
                         <p class="card-text">{{ property.description }} </p>
                         <p class="text-small">{{ property.price }} € </p>
                    </div>
               </div>

          {% endfor %}

     </div>
</div>


{% endblock %}