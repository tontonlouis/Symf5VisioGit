<!doctype html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    {% if contact.property %}
        <title>Demande de reseignement : {{ contact.property.name }}</title>
    {%  else %}
        <title>Demande de Contact</title>
    {% endif %}
</head>
<body>
    {% if contact.property %}
    <p>M. ou Mme {{ contact.firstname }} {{ contact.lastname }} demande des reseignements pour le bien {{ contact.property.name }}
        qui a pour ID {{ contact.property.id }}
    </p>
    {% else %}
        <p>M. ou Mme {{ contact.firstname }} {{ contact.lastname }} demande a être contacter
        </p>
    {% endif %}
    <p>
        Voici leur demande : <br>
        {{ contact.message }}
    </p>

    <p>
        Vous pouvez la recontacter par téléphone au {{ contact.phone }} ou par email à l'adresse {{ contact.email }}
    </p>
</body>
</html>